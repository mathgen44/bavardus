# Bavardus — dépendances du moteur.
# Épinglées par minimum compatible : ce dépôt vise l'auto-hébergement, une
# mise à jour surprise ne doit pas casser une installation qui tourne.

PyYAML>=6.0
httpx>=0.27

# Interface web (étape 6) — déjà listées pour figer la cible.
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
itsdangerous>=2.2

# Sources d'événements (étapes 3 et 5).
websockets>=12.0
# R1/G10 : Streamlabs expose un Socket.IO ancien (Engine.IO v3).
# python-socketio 5.x NE PEUT PAS s'y connecter. Ne pas « moderniser ».
python-socketio[client]==4.6.1
python-engineio==3.14.2
